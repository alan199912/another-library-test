import { DayMonthFormatPipe } from './day-month-format.pipe';

describe('DayMonthFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DayMonthFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
