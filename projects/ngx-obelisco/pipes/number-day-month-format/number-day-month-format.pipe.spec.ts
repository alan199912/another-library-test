import { NumberDayMonthFormatPipe } from './number-day-month-format.pipe';

describe('NumberDayMonthFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberDayMonthFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
